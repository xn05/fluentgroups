<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FluentGroupsUI.Views.CreateGroupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Create New Group" FontSize="28" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="16">
                <!-- Group Name -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Group Name" FontSize="14" FontWeight="SemiBold"/>
                    <TextBox x:Name="GroupNameInput" PlaceholderText="Enter group name"/>
                </StackPanel>

                <!-- Group Description -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Description" FontSize="14" FontWeight="SemiBold"/>
                    <TextBox x:Name="GroupDescriptionInput" PlaceholderText="Enter group description" Height="80" TextWrapping="Wrap"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" MinWidth="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Cancel"/>
            <Button Grid.Column="2" Content="Create" Style="{ThemeResource AccentButtonStyle}"/>
        </Grid>
    </Grid>
</UserControl>

